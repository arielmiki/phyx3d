## What changed

## How I verified it
<!-- tests added/updated, commands run, screenshots for UI changes, real prints compared -->

- [ ] `pnpm typecheck`, `pnpm test` and `pnpm build` pass
- [ ] Physics/solver changes include a test with a known answer
- [ ] Docs updated (README, docs/, skill) if behaviour changed
